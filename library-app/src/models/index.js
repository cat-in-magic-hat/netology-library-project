const Book = require('./book');
const BookComment = require('./book-comment');
const ErrorResult = require('./error-result');
const User = require('./user');

module.exports = {
    Book,
    BookComment,
    ErrorResult,
    User
}
